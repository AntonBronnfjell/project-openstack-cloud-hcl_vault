# ============================================================================
# Vault Service Leaf Module - Main Configuration
# ============================================================================
# This module deploys HashiCorp Vault as a high-availability service
# on OpenStack infrastructure. It creates multiple instances for zero-downtime
# deployments and integrates with the root repository for Traefik routing.
#
# Local values are defined in docker-deploy.tf and outputs.tf
